server:
  port: 8080
spring:
  application:
    name: student
    ##DB name:student
  datasource:
    username: rajmi
    password: password
    url: jdbc:postgresql://localhost:5432/student
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    show-sql: 'true'

